# 预设列表项原生化调整
- `src/ui/preset-item-ui.ts`：预设项 DOM 简化为左右两列，左侧包含名称输入与信息行，右侧仅保留操作按钮；按钮改用 Obsidian `setIcon` 渲染，并添加 `clickable-icon` 原生类，去掉内联 SVG。
- `styles.css`：预设项容器改为左右排布的扁平列表行，仅保留底部分割线与悬停背景；名称输入改为透明边框的“伪文本”风格，悬停/聚焦时才显色；辅助信息降级为 `text-muted`，ID 使用等宽字体；操作区只保留布局间距，移除自定义按钮外观。